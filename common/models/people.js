'use strict';

module.exports = function(People) {

	// People.greet = function(msg, cb) {
	// 	process.nextTick(function() {
	// 		msg = msg || 'hello';
	// 		cb(null, 'Angular and Loopback is now Configured For ' + msg);
	// 	});
	// };

	// People.login = function(msg, cb) {
	// 	process.nextTick(function() {
	// 		msg = msg || 'hello';
	// 		cb(null, 'shouldnt be called from here... ' + msg);
	// 	});
	// };
};
